{
  "name": "utils-array",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "dependencies": {
    "react-grid-layout-component": "^1.0.0",
    "use-bounding-client-rect": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^3.5.3"
  }
}
