{
  "private": true,
  "name": "soliquid",
  "description": "framework",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "tsc -b packages/front-react/components/grid-layout-component",
    "test": "lerna run test",
    "clean": "rm -rf packages/**/**/**/lib"
  },
  "peerDependencies": {
    "react": ">=16.8.0"
  },
  "devDependencies": {
    "@types/node": "^8.0.28",
    "lerna": "^2.9.0",
    "typescript": "^3.6.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
