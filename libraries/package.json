{
  "name": "@wb/libraries",
  "version": "0.0.5",
  "description": "Standalone libraries package",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/willybrauner/libraries.git"
  },
  "dependencies": {
    "@wb/react-grid-layout-component": "^0.0.5",
    "@wb/use-bounding-client-rect": "^0.0.5",
    "@wb/use-window-size": "^0.0.5",
    "@wb/utils-array": "^0.0.5"
  },
  "devDependencies": {
    "typescript": "^3.7.3"
  },
  "keywords": [
    "react",
    "libraries",
    "node",
    "typescript"
  ],
  "publishConfig": {
    "access": "public"
  },
  "author": "Willy Brauner",
  "license": "MIT"
}
